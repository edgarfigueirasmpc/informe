{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "informe",
      "comentario": "Equivale a `npm run dev`. Node vive en el entorno conda `informe` y no en el PATH del sistema, así que se llama al binario por su ruta y a Vite por su entrada: `npm` no arranca porque su propio shebang busca `node` en el PATH.",
      "runtimeExecutable": "/Users/edgar/miniforge3/envs/informe/bin/node",
      "runtimeArgs": ["node_modules/vite/bin/vite.js"],
      "port": 5173
    }
  ]
}
